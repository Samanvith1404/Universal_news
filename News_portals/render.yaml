services:
  - type: web
    name: pharma-intelligence
    env: python
    buildCommand: pip install -r requirements_server.txt
    startCommand: uvicorn pharma_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
